# -----------------------------------------------------------------------------
# Example environment file for process_folders_rclone_bq.sh
# Copy this to .env and adjust values, then:  source .env
# -----------------------------------------------------------------------------

# REQUIRED: Comma-separated list of folder prefixes inside the Azure container
# Example: PROCESS_FOLDERS="boardex,trucost,ofac"
PROCESS_FOLDERS=""

# REQUIRED: GCP bucket name (no gs:// prefix)
# Example: GCP_BUCKET="my-gcs-bucket"
GCP_BUCKET=""

# Optional: Azure Blob container name (default: merged-parquet)
AZ_CONTAINER="merged-parquet"

# Optional: Rclone remote name for Azure Blob (default: azure)
# This must match what you configured with `rclone config`
AZ_REMOTE="azure"

# Optional: Rclone remote name for Google Cloud Storage (default: gcs)
# This must match what you configured with `rclone config`
GCS_REMOTE="gcs"

# Optional: rclone mode
#   - sync = mirror (will delete files in GCS that don't exist in Azure)
#   - copy = add/update only (will not delete from GCS)
RCLONE_MODE="sync"

# ----------------- BigQuery settings ----------------------------------------

# GCP project for BigQuery (defaults to current gcloud config project)
BQ_PROJECT=""

# BigQuery dataset (default: cq_masters)
BQ_DATASET="cq_masters"

# BigQuery location (default: US)
BQ_LOCATION="US"

# BigQuery write disposition
#   - WRITE_TRUNCATE (default) = replace table
#   - WRITE_APPEND             = append rows
#   - WRITE_EMPTY              = only create if table empty
BQ_WRITE_DISPOSITION="WRITE_TRUNCATE"

# Concurrency of BigQuery loads (default: 4)
BQ_CONCURRENCY="4"

# Optional: skip BigQuery step entirely (true/false, default: false)
SKIP_BQ="false"

# ----------------- Retry / Misc settings -----------------------------------

# Max retries for BigQuery loads (default: 3)
MAX_RETRIES="3"

# Seconds to sleep between retries (default: 4)
SLEEP_BETWEEN_RETRY="4"

# Working directory for metadata/logs (default: /tmp/process_folders_rclone)
WORK_DIR="/tmp/process_folders_rclone"

# -----------------------------------------------------------------------------
